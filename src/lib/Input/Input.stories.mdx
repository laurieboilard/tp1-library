import { Meta, Props, Canvas, Story } from '@storybook/addon-docs';
import Input from './Input';

<Meta title="Example/Input" component={Input} />

# Example Component Demo

## Input props
<Props of={Input} />

## Description

`Input` is widget for getting the user input is a text field. Keyboard and mouse can be used for providing or changing data.

## Demo

<Canvas>
  <Story name="Small">
    <Input size="small" placeholder="small size" prefix={<UserOutlined />} />
  </Story>
  <Story name="Medium">
    <Input size="medium" placeholder="small size" prefix={<UserOutlined />} />
  </Story>
    <Story name="Large">
    <Input size="large" placeholder="small size" prefix={<UserOutlined />} />
  </Story>
</Canvas>
